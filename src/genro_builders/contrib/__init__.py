# Copyright 2025 Softwell S.r.l. - SPDX-License-Identifier: Apache-2.0
"""Contributed builders and renderers — ready-to-use domain-specific components.

These are shipped with genro-builders but are not part of the core framework.
They are optional and only loaded when explicitly imported.

Available:
    - ``genro_builders.contrib.data`` — DataBuilder (structured data schemas)
    - ``genro_builders.contrib.html`` — HtmlBuilder, HtmlRenderer (W3C HTML5)
    - ``genro_builders.contrib.live`` — LiveSession, LiveServer, LiveProxy (remote control)
    - ``genro_builders.contrib.markdown`` — MarkdownBuilder, MarkdownRenderer
    - ``genro_builders.contrib.svg`` — SvgBuilder, SvgRenderer (W3C SVG)
    - ``genro_builders.contrib.xsd`` — XsdBuilder (schema-driven XML from XSD)
    - ``genro_builders.contrib.yaml`` — YamlRendererBase (YAML output)
"""
